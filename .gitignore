# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 工作产物（skill 运行时生成，不入库）
# ⚠️ 不要用 `30-FS-*.md` 这种无锚点通配 —— 会把 skill 自带的模板
#    `fs-quick-design/references/03-产出模板/30-FS-模板.md` 一起排除掉（已踩过）。
#    这里显式排除「模板」二字，只忽略运行产物。
30-FS-*.md
!30-FS-模板.md
figs.json
out/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 备份（skills 根目录下曾因同名 SKILL.md 遮蔽正本）
*backup*/
*.bak
*.sha256

# 敏感信息（绝不入库）
.credentials.yaml
*.credentials.*
.env
.env.*
mcp.json
